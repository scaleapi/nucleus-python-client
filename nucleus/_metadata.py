# In this file, we can store attributes/package metadata in the form __key__ = val. This is useful
# as a single point of reference and a place to make variables visible to both the package, and the
# setup.py file.
__version__ = '0.1.0'
